services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: fampilot
      POSTGRES_PASSWORD: fampilot
      POSTGRES_DB: fampilot
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
volumes:
  pgdata:
